from setuptools import setup

setup(name='shipNavEnv',
      version='1.1.1',
      install_requires=['gym','numpy','pybox2d', 'PyVirtualDisplay', 'pyyaml', 'pyglet', 'optuna', 'pathfinding']#And any other dependencies required
)
